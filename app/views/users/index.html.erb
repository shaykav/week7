<h1>Users</h1>

<% @users.each do |user| %>
  <h2><%= user.name %></h2>
  <p> <%= user.bio %></p>
  <%= image_tag user.picture %>
  <p> <%= user.comments.count %></p>
  <p> <%= user.followers.count %></p>
<% end  %>
